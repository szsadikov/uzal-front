import ConfigProvider, {
	ConfigConsumer,
	ConfigContext,
	defaultConfig,
	useConfig
} from './ConfigProvider'

export type { Config } from './ConfigProvider'

export { ConfigConsumer, ConfigContext, ConfigProvider, defaultConfig, useConfig }

export default ConfigProvider
